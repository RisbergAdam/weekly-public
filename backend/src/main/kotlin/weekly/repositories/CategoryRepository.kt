package weekly.repositories

/*fun Repository<ProductCategory>.findByName(name: String) = this.findNative("""
    SELECT * FROM product_category WHERE name = ?
    """.trimIndent())
        .setParameter(1, name)
        .findOne()*/